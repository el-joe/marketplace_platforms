import type { ReturnDetail } from "../../orders/helpers/types";

type Props = {
  returnDetail: ReturnDetail;
};

// Data wiring only — UI for this screen hasn't been designed yet.
export default function ReturnDetails({ returnDetail: _returnDetail }: Props) {
  return null;
}
