/**
 * Partner panel (partner.noon.loc) entrypoint.
 *
 * Re-uses the shared app.js globals (jQuery, Alpine, Toastify, Swal).
 * Partner-specific UI code goes here or in dedicated component files.
 */
import '../app.js';
import '../components/layout.js';
